apply plugin: 'git-dependencies'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.batcha.gradle.plugins:git-dependencies:0.2'
        compile {
            ext.git = 'git@github.com:50ButtonsEach/fliclib-android.git'
        }
    }
}
